# Pinned to "known-good floor, < next breaking release" (audit B13, 2026-09-03).
# Verified set: fastapi 0.141.1, uvicorn 0.52.4, python-multipart 0.0.32,
# pydantic 2.13.5, pydantic-settings 2.15.0, numpy 2.0.2, scipy 1.17.1,
# librosa 0.11.0, soundfile 0.14.0, pyworld 0.3.5, mido 1.3.3, pyfluidsynth 1.4.0,
# supabase 2.31.0, boto3/botocore 1.43.87, PyJWT 2.13.0, cryptography 50.0.1,
# httpx 0.28.1, slowapi 0.1.10, sentry-sdk 2.68.1, google-auth 2.57.0.
# Bump a ceiling only after running the test suite against the new major.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9,<0.1
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
numpy>=1.26,<2.1
scipy>=1.11,<2
librosa>=0.10.1,<0.12
soundfile>=0.12,<0.15
# Autotune (/autotune): WORLD vocoder — f0-replacement resynthesis with
# formant preservation. cp311 manylinux wheels (no compiler needed in Docker).
pyworld>=0.3.4,<0.4
mido>=1.3,<2
# Optional - SoundFont preview via FluidSynth. Requires libfluidsynth-3.dll
# (bundled portable build under backend/bin/ is auto-loaded by app.render).
pyfluidsynth>=1.3.0,<2
# --- P0 persistence + commerce + ops ----------------------------------------
supabase>=2.5.0,<3
boto3>=1.34,<2
botocore>=1.34,<2
pyjwt[crypto]>=2.8,<3
cryptography>=42.0,<51
httpx>=0.27,<1
slowapi>=0.1.9,<0.2
sentry-sdk[fastapi]>=2.0,<3
google-auth>=2.29,<3
